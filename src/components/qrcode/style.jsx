import styled from 'styled-components';
import { utilies } from '../../styledUtilies/utilesVariables';

export const StyledQR = styled.a`
    width: 85%;
    margin:0  0 40px 35px;
    ${utilies.centrar}
`